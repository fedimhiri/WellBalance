{{ form_start(form, {'attr': {'class': 'row g-3'}}) }}
{{ form_errors(form) }}

<div class="col-md-6">{{ form_row(form.titre) }}</div>
<div class="col-md-6">{{ form_row(form.dateRdv) }}</div>
<div class="col-md-6">{{ form_row(form.lieu) }}</div>
<div class="col-md-6">{{ form_row(form.type) }}</div>
<div class="col-12">{{ form_row(form.notes) }}</div>
<div class="col-md-6">{{ form_row(form.statut) }}</div>

{{ form_rest(form) }}

<div class="col-12">
  <button class="btn btn-success">{{ button_label|default('Enregistrer') }}</button>
</div>

{{ form_end(form) }}
