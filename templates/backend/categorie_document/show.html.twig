{% extends 'backend/baseend.html.twig' %}

{% block title %}Détails catégorie{% endblock %}

{% block body %}
<div class="container-fluid">
    <h4 class="mb-4">Détails catégorie de document</h4>
    <div class="card shadow-sm">
        <div class="card-body">
            <table class="table table-bordered align-middle mb-0">
                <tbody>
                    <tr><th style="width: 220px;">ID</th><td>{{ categorie_document.id }}</td></tr>
                    <tr><th>Description</th><td>{{ categorie_document.description }}</td></tr>
                </tbody>
            </table>
            <div class="d-flex gap-2 mt-3">
                <a class="btn btn-secondary" href="{{ path('app_categorie_document_index') }}">← Retour</a>
                <a class="btn btn-warning" href="{{ path('app_categorie_document_edit', {id: categorie_document.id}) }}">✎ Modifier</a>
                <form method="post" action="{{ path('app_categorie_document_delete', {id: categorie_document.id}) }}" onsubmit="return confirm('Confirmer la suppression ?');" class="d-inline">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_document.id) }}">
                    <button class="btn btn-danger" type="submit">🗑 Supprimer</button>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}
