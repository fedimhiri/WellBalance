<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <title>Mon plan - {{ plan.objectif }}</title>
  <style>
    body { font-family: DejaVu Sans, Arial, sans-serif; font-size: 12px; color: #222; }
    .header { border-bottom: 2px solid #333; padding-bottom: 10px; margin-bottom: 15px; }
    .title { font-size: 18px; font-weight: bold; margin: 0; }
    .sub { color: #666; margin-top: 4px; }
    .box { border: 1px solid #ddd; padding: 10px; border-radius: 6px; margin-bottom: 12px; }
    .table { width: 100%; border-collapse: collapse; margin-top: 10px; }
    .table th, .table td { border: 1px solid #ddd; padding: 7px 8px; }
    .table th { background: #f3f3f3; text-align: left; }
  </style>
</head>
<body>

<div class="header">
  <p class="title">Mon plan nutrition — {{ plan.objectif }}</p>
  <p class="sub">Du {{ plan.dateDebut|date('d/m/Y') }} au {{ plan.dateFin|date('d/m/Y') }} • Généré le {{ generatedAt|date('d/m/Y H:i') }}</p>
</div>

{% set stats = plan.getStats() %}
<div class="box">
  <strong>Résumé</strong><br>
  Total calories : <strong>{{ stats.total_calories }}</strong> kcal<br>
  Moyenne / repas : <strong>{{ stats.moyenne_calories }}</strong> kcal<br>
  Nombre de repas : <strong>{{ stats.nombre_repas }}</strong>
</div>

<h3 style="margin: 10px 0 6px;">Mes repas</h3>

{% if plan.repas|length == 0 %}
  <p>Aucun repas enregistré.</p>
{% else %}
  <table class="table">
    <thead>
      <tr>
        <th>Date</th>
        <th>Type</th>
        <th>Calories</th>
        <th>Description</th>
      </tr>
    </thead>
    <tbody>
      {% for r in plan.repas %}
        <tr>
          <td>{{ r.dateRepas ? r.dateRepas|date('d/m/Y H:i') : '-' }}</td>
          <td>{{ r.typeRepas }}</td>
          <td>{{ r.calories }} kcal</td>
          <td>{{ r.description }}</td>
        </tr>
      {% endfor %}
    </tbody>
  </table>
{% endif %}

</body>
</html>
