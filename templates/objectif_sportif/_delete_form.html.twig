<form method="post" action="{{ path('app_objectif_sportif_delete', {'id': objectif_sportif.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objectif_sportif.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
